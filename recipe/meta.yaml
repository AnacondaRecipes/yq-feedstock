{% set name = "yq" %}
{% set version = "4.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: ab3fc344dc4a39d833d328f080a6e794c1cb7243b05c0076ac1edce3d22caabd

build:
  number: 0
  entry_points:
    - yq=yq:cli
    - xq=yq:xq_cli
    - tomlq=yq:tq_cli
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  skip: true  # [py<38]

requirements:
  host:
    - pip
    - python
    - setuptools_scm >=7
    - hatchling
    - hatch-vcs
  run:
    - python
    - pyyaml >=5.3.1
    - xmltodict >=0.11.0
    - tomlkit >=0.11.6
    - argcomplete >=1.8.1

test:
  source_files:
    - test
  imports:
    - yq
  commands:
    - pip check
    - yq --help
    - xq --help
    - tomlq --help
    # Windows error: E   AttributeError: module 'os' has no attribute 'EX_OK'
    - pytest -v test/test.py  # [not win]
  requires:
    - pip
    - pytest
    - jq

about:
  home: https://github.com/kislyuk/yq
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: Command-line YAML/XML processor - jq wrapper for YAML/XML documents
  description: |
    yq: Command-line YAML/XML/TOML processor - jq wrapper for YAML, XML, TOML documents
  doc_url: https://kislyuk.github.io/yq
  dev_url: https://github.com/kislyuk/yq

extra:
  recipe-maintainers:
    - sodre
    - xhochy
